"""Test suite for FilaManBambuUsage.

Runs on the standard library alone, so it works without installing anything:

    python3 -m unittest discover -s tests -t .

pytest picks the same tests up if it is available.
"""
